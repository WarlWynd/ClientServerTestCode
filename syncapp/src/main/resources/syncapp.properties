# ─────────────────────────────────────────────────────────────
#  Sync App Configuration
#  Distribute this file alongside the app, or bake values in.
# ─────────────────────────────────────────────────────────────

# Branding
app.name=Adventure Friends Sync
app.version=1.0.0
corp.name=Wynd Farm

# Asset server — the public URL users connect to
asset.url=http://147.136.236.127:9877

# Local installation directory (supports ${user.home} and ${appdata})
# Files are placed in subdirectories matching the server asset types.
install.dir=${user.home}/.adventure-friends
