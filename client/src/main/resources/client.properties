# ─────────────────────────────────────────────
#  Game Client Configuration
# ─────────────────────────────────────────────

# Server address and UDP port
server.host=192.168.1.153
server.port=9876

# Local UDP port (0 = OS-assigned ephemeral port)
client.port=0

# Client version — must match GitHub release tag (without 'v')
app.version=1.0.0
